# Calls the reusable workflows defined once in lemonfiber/spec (Q-R56).
name: ci
on: [pull_request]
permissions:
  contents: read

jobs:
  # `pull-requests: write` is what lets the gate close a pull request whose
  # citation is missing or names an identifier that does not exist, rather than
  # only going red at it. A reusable workflow's permissions block is a request,
  # and a caller granting less than it asks for fails the whole run before any
  # job starts — every required context silent, and nothing to report against.
  # The workflow-level `contents: read` is the default for a job that declares
  # nothing, and a job-level block replaces that default outright, so
  # `contents: read` is restated here rather than inherited.
  spec-check:
    permissions:
      contents: read
      pull-requests: write
    uses: lemonfiber/spec/.github/workflows/spec-check.yml@7685ff1dd36aafe05bd6ce9b1bd642f052e7b599 # main
  hygiene:
    uses: lemonfiber/spec/.github/workflows/hygiene.yml@7685ff1dd36aafe05bd6ce9b1bd642f052e7b599 # main
  security:
    uses: lemonfiber/spec/.github/workflows/security.yml@7685ff1dd36aafe05bd6ce9b1bd642f052e7b599 # main
  dco:
    uses: lemonfiber/spec/.github/workflows/dco.yml@7685ff1dd36aafe05bd6ce9b1bd642f052e7b599 # main
  attribution:
    uses: lemonfiber/spec/.github/workflows/attribution.yml@7685ff1dd36aafe05bd6ce9b1bd642f052e7b599 # main
  commitlint:
    uses: lemonfiber/spec/.github/workflows/commitlint.yml@7685ff1dd36aafe05bd6ce9b1bd642f052e7b599 # main
  labeler:
    permissions:
      contents: read
      pull-requests: write
    uses: lemonfiber/spec/.github/workflows/labeler.yml@7685ff1dd36aafe05bd6ce9b1bd642f052e7b599 # main
